import React from "react";
import ReactDOM from "react-dom";

const parent= React.createElement("h1",{},"Iam headung");

const root = ReactDOM.createroot(React.createElementById(root));
 root.render(parent);